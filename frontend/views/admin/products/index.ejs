<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/catalog.css">
</head>
<body>
  <main class="page">
    <header class="catalog-header">
      <div>
        <a href="/admin/dashboard" class="back-link">Dashboard</a>
        <h1>Products</h1>
      </div>
      <a href="/admin/products/new" class="button-link">Add product</a>
    </header>

    <section class="cart-panel">
      <% if (products.length === 0) { %>
        <p class="empty-message is-visible">No products found.</p>
      <% } else { %>
        <div class="cart-table-wrap">
          <table class="cart-table">
            <thead>
              <tr>
                <th>Name</th>
                <th>Category</th>
                <th>Price</th>
                <th>Stock</th>
                <th>Image</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% products.forEach((product) => { %>
                <tr>
                  <td><%= product.product_name %></td>
                  <td><%= product.category_name %></td>
                  <td>PHP <%= product.price.toFixed(2) %></td>
                  <td><%= product.stock_quantity %></td>
                  <td><%= product.image || '-' %></td>
                  <td>
                    <a href="/admin/products/<%= product.id %>/edit">Edit</a>
                    <form action="/admin/products/<%= product.id %>/delete" method="POST" class="inline-form">
                      <button type="submit" class="remove-button">Delete</button>
                    </form>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      <% } %>
    </section>
  </main>
</body>
</html>
